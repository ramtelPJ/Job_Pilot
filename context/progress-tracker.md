# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 5 — Dashboard
**Last completed:** 17 Analytics Charts — PostHog Data
**Next:** All 17 build-plan.md features complete

---

## Progress

### Phase 1 — Foundation

- [x] 01 Homepage
- [x] 02 Auth
- [x] 03 PostHog Initialization
- [x] 04 Database Schema

### Phase 2 — Profile Page

- [x] 05 Profile Page — Full UI
- [x] 06 Profile Save Logic
- [x] 07 AI Profile Extraction from Resume
- [x] 08 Resume PDF Generation from Profile

### Phase 3 — Find Jobs Page

- [x] 09 Find Jobs Page — Full UI
- [x] 10 Adzuna Job Discovery
- [x] 11 Filter + Sort + Pagination

### Phase 4 — Job Details Page

- [x] 12 Job Details Page — Full UI
- [x] 13 Company Research Agent

### Phase 5 — Dashboard

- [x] 14 Dashboard Page — Full UI
- [x] 15 Stats Bar — Real Data
- [x] 16 Recent Activity — Real Data
- [ ] 17 Analytics Charts — PostHog Data

---

## Decisions Made During Build

- Homepage built pixel-matched against `context/designs/landing-page.png`. All CTAs ("Get Started", "Find Your First Match", "Start for free") link to `/login` — since auth (Feature 02) isn't built yet, the conditional "if authenticated -> /dashboard" redirect from the build plan is deferred. Revisit once auth exists.
- Added `--color-accent-deep: #4a2ec5` token to `@theme` in globals.css — needed for the logo's 45deg gradient endpoint per ui-tokens.md's Logo spec, which isn't covered by an existing token.
- Dark CTA buttons ("Get Started", "Start for free") use `bg-text-slate` (#272835) — sampled directly from the design, closest existing token match, not the purple `--color-accent`.
- Secondary hero/CTA button ("Find Your First Match") in the Hero uses `bg-accent-light`; the same button in the bottom CTA section uses `bg-surface` + `border-border` (standard secondary) — these differ pixel-for-pixel in the source design, not a mistake.
- Jobs-table mock (in "Manage Your Job Search" section) and dashboard preview mock (browser-chrome card in hero) are static illustrative data hand-built with Tailwind, not live components — no image assets existed in `public/`, and the design itself is a coded mockup, not a screenshot.
- Testimonial avatar for "Tom Wilson" uses an initials placeholder (no real photo asset available).
- Second highlighted feature-list item border ("AI-Powered Job Matching") uses `border-success-dark` (#007a55, sampled from design) — not accent, unlike the first feature section's highlighted item which does use `border-accent-dark`.
- **InsForge SDK reality check (Feature 02):** `context/architecture.md` and `context/library-docs.md` originally documented a `@insforge/ssr` package with `createBrowserClient(url, key)` / `createServerClient(url, key, {cookies})` — this doesn't exist. The MCP `fetch-docs`/`fetch-sdk-docs` tools' "auth-sdk" doc is also generic and doesn't mention SSR at all. The real, correct pattern (confirmed by installing `@insforge/sdk` and reading its bundled `SDK-REFERENCE.md` + `.d.ts` files) is: SSR helpers live at the `@insforge/sdk/ssr` and `@insforge/sdk/ssr/middleware` subpaths of the one `@insforge/sdk` package. Both context docs have been rewritten to match. **Lesson: for this SDK, the package's own bundled `SDK-REFERENCE.md` is more authoritative than the MCP `fetch-docs` tool for exact method/import signatures.**
- Auth is entirely server-initiated (no client-side auth SDK calls): `/login` is a Server Component whose OAuth buttons are `<form>`s bound to the `signInWithOAuth` Server Action (`actions/auth.ts`), which uses `createAuthActions()` to call InsForge and write session cookies in one step — raw tokens never reach client JS or app code. The PKCE `codeVerifier` is bridged across the redirect via a short-lived httpOnly cookie (`insforge_oauth_verifier`), read back by `app/api/auth/callback/route.ts`.
- Dropped the `(auth)/callback/page.tsx` from architecture.md's original folder plan — the callback is a **Route Handler** (`app/api/auth/callback/route.ts`), not a page, since it must write cookies and issue a redirect before any React render happens.
- Next.js 16 renamed `middleware.ts` → `proxy.ts` (function `middleware` → `proxy`) — confirmed against `node_modules/next/dist/docs/.../file-conventions/proxy.md` per AGENTS.md's "read the docs before writing code" instruction. Route protection lives in `proxy.ts` at the project root, using `updateSession()` from `@insforge/sdk/ssr/middleware`.
- Added `NEXT_PUBLIC_APP_URL` env var (not in the original code-standards.md table) — needed to build an absolute OAuth `redirectTo` URL from a Server Action, which has no `request` object to read the origin from.
- Homepage's "redirect to /dashboard if already authenticated" (from build-plan.md's Homepage Logic) is still deferred — `/dashboard` doesn't exist as a page yet (Feature 14, Phase 5). Revisit once it's built; redirecting there now would 404.
- Verified end-to-end with Playwright against the real InsForge backend: clicking "Continue with Google" on `/login` navigates all the way to a real `accounts.google.com` sign-in screen (proves env vars, the Server Action, and `createAuthActions` are all wired correctly). Also verified `proxy.ts` redirects unauthenticated `curl` requests to `/dashboard`, `/profile`, and `/find-jobs` back to `/login` (307).

---

- **PostHog (Feature 03):** found `lib/posthog-server.ts` and PostHog capture calls already present in `actions/auth.ts`/`app/api/auth/callback/route.ts` on disk before this feature was formally started (event names `sign_in_initiated`, `sign_in_completed`, `sign_in_failed`, `sign_out` — not in code-standards.md's original 4-event allowlist). Rather than rip out already-working code, formally added these to the approved events table (code-standards.md now lists 9), since the rule's own remedy is "update this list first," not "never add events."
- Invoked the installed `integration-nextjs-app-router` PostHog skill before writing any PostHog code (per AGENTS.md's "load the installed skill first" rule). It corrected assumptions baked into `architecture.md`/`library-docs.md`/`build-plan.md`: no `lib/posthog-client.ts` file, no React provider — current convention is `instrumentation-client.ts` at the project root (Next.js 15.3+ file convention, confirmed against `node_modules/next/dist/docs/`). Env var is `NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN`, not the `NEXT_PUBLIC_POSTHOG_KEY` the docs originally had.
- Added `components/analytics/IdentifyUser.tsx` (root layout, server-fed) and `components/analytics/ResetAnalytics.tsx` (`/login?loggedOut=1`) to properly wire `identify()`/`reset()` given our OAuth flow is entirely server-initiated (no client-side sign-in/out code to hook into directly, unlike the typical client-side-form example in PostHog's own docs).
- Added `cta_clicked` (properties: `cta`, `location`) as the one new client-side event, via a shared `components/analytics/TrackedLink.tsx` — instrumented on the 5 marketing CTA buttons (Navbar, Hero ×2, CTASection ×2). Did not touch the original 4 approved events (job_search_started, job_found, profile_completed, company_researched) — none of their features exist yet.
- `getPostHogClient()` is a module-level singleton (not per-request) — every call site uses `await posthog.flush()`, never `shutdown()`, per the PostHog skill's explicit singleton-vs-per-request rule.
- `app/layout.tsx` is now `async` and calls `createInsforgeServer().auth.getCurrentUser()` on every request (needed so `IdentifyUser` can identify an already-logged-in user on page refresh) — this makes every page dynamic, consistent with code-standards.md's "uncached by default" rule, not a new pattern.

- **Database Schema (Feature 04):** designed via `/architect` — spec at `docs/specs/0001-core-data-schema.md`. Formalized the schema already drafted in architecture.md rather than redesigning from scratch, but resolved a real gap: `jobs.source`/`run_id` was documented as nullable for a `'url'` source, even though manual URL import is explicitly out of scope — dropped `'url'`, made `run_id` NOT NULL, and added a database CHECK constraint (not just a column default) to actually enforce it. A same-model cross check on the spec caught 2 more places where an enum-like column (`agent_runs.status`, `agent_logs.level`) was documented as fixed values but had no CHECK — fixed before building. Built directly against the live InsForge backend via the MCP `run-raw-sql`/`create-bucket` tools and verified with `get-table-schema` afterward (all 4 tables, 8 indexes, RLS + policies, 3 CHECK constraints, and the private `resumes` bucket all confirmed present).
- **Profile Page (Feature 05):** built pixel-matched against `context/designs/profile.png`, mock data only, no save/persistence logic yet (Feature 06). Two design findings discovered from this mockup that weren't visible in the homepage design, applied retroactively to the shared `Navbar`: (1) app pages show a lucide icon next to each nav label and an active-tab underline (`authenticated` prop added, defaults to `false` so the homepage is unaffected); (2) there's no "Start for free"/sign-out CTA on an authenticated navbar in this design — left absent rather than invented. Added `--color-error-light: #fee2e2` to `@theme` (ui-tokens.md had no light-red token for the completion ring track / missing-field tags). The work-experience "currently working here" checkbox samples as a vivid unstyled-browser blue (`#0175FF`, no match to any project token) — judged as a mockup artifact, not a deliberate color choice, so it's styled with brand purple (`accent-[var(--color-accent)]`) instead of replicated literally. Text/textarea inputs get `bg-surface-secondary` when they hold a value and plain `bg-surface` when empty (matching the design's own filled-vs-placeholder visual language exactly); `<select>` elements are always `bg-surface` regardless of value, since the design shows dropdowns as white even when a real option is selected. Verified visually with Playwright (temporarily removed `/profile` from `proxy.ts`'s protected list to screenshot as a Server Component would render for a signed-in user, then restored protection immediately after — confirmed with a follow-up `curl` that the 307 redirect came back).
- **Profile Save Logic (Feature 06):** wired `actions/profile.ts`'s `saveProfile()` Server Action to the real InsForge `profiles` table via `.upsert()` (not `.update()`) — no signup trigger creates a `profiles` row automatically when a user first signs in via OAuth, so the very first save for a new user has to be an insert, not an update. Refactored `SkillsInput` and `WorkExperienceSection` from Feature 05 to be fully controlled components (they owned their own `useState` before) so `ProfileForm` can be the single source of truth and assemble one `FormData` payload (including the optional resume `File`) for one Server Action call.
- **Completion percentage is computed, not stored** (`lib/profile-completion.ts`), deliberately deviating from build-plan.md's literal "completion percentage and missing fields calculated and saved" wording — only the real `is_complete` boolean column (already in the Feature 04 schema) is persisted. This follows the same "never store a derived value you can cheaply compute at read time" principle the `/architect` skill's FEATURE mode surfaced during the database schema work; there's no `completion_percentage` column to add a stale value to. The 11-section algorithm was sanity checked against the profile.png mock's own example (missing Phone/Location/Education) and produced 73% versus the mock's 70% — close enough to confirm the field weighting is right, given the mock's number was just illustrative.
- `job_titles_seeking` and `preferred_locations` are `text[]` columns (Feature 04 schema) but the UI (matching profile.png) presents them as single comma-separated text inputs — the Server Action splits/joins on save and the page maps array-to-string on load, rather than changing the schema or the UI to match each other more directly.
- `cover_letter_tone` (a real column, and named in build-plan.md's Feature 05 spec text) has no form field anywhere — profile.png's actual Job Preferences section only shows Job Titles Seeking / Remote Preference / Salary Expectation / Preferred Locations, ending at Save Profile. Since the design image was the source of truth for Feature 05's UI, the field was correctly left out then; it stays unset (not written by `saveProfile`) until/unless a future design revision adds it.
- Verified without real OAuth credentials (can't complete a real Google/GitHub login from an automated session): confirmed the completion algorithm directly against the parsed profile shape (a plain data check, no server needed), then verified all the interactive behavior (typing, add/remove skill tags, work-experience add-role/remove-role, the "currently working here" toggle enabling/disabling End Date) via a temporary unauthenticated test route rendering the same components with mock data — deleted immediately after. Submitting the real save action from that route correctly came back with "You must be signed in to save your profile," which is itself a real, valuable confirmation that the Server Action's auth guard rejects unauthenticated writes against the live backend, not a mocked one.
- **Resume view 401 fix (post-Feature 06):** clicking "View current resume" 401'd with `AUTH_INVALID_CREDENTIALS` — confirms the Follow-up flagged in `docs/specs/0001-core-data-schema.md`: the `resumes` bucket is private, and its object URL needs a bearer token on every request, which a plain `<a href>` navigation can't provide. Fixed by having `ResumeUpload` fetch through `insforge.storage.from('resumes').download(userId/resume.pdf)` (the browser SDK client, which attaches the access-token cookie automatically) and open a `URL.createObjectURL()` blob instead of linking to the raw URL directly.
- **AI provider switch: OpenAI GPT-4o → Claude (all features, Feature 07).** `.env.local` only had `CLAUDE_API_KEY`, no OpenAI key, while every AI-related doc (`project-overview.md`, `architecture.md`'s stack table, `library-docs.md`, `build-plan.md` Features 07/08/10/13) was written entirely around GPT-4o. Rather than build Feature 07 on a provider with no key configured, or introduce a mixed-provider stack, switched the whole project's documented AI provider to Claude — updated all four docs (every "GPT-4o" mention, the stack table row, the approved-dependencies line, the env var table) so future features (10, 13) are speced consistently. Loaded the `claude-api` skill before writing any code, per this project's own "load the installed skill first" rule.
- Installed `@anthropic-ai/sdk`, `zod` (already approved, now actually used), and `pdf-parse`. **`pdf-parse` is v2**, not the v1 shape `library-docs.md` originally documented (`import pdf from 'pdf-parse'; pdf(buffer)`) — the real v2 API is class-based (`new PDFParse({ data: buffer }).getText()`, with a mandatory `await parser.destroy()` to free memory) — caught by reading the installed package's own README rather than trusting recalled training data, same pattern as the InsForge SDK reality-check earlier in this build. `library-docs.md`'s pdf-parse section is rewritten to match.
- **Structured extraction uses `client.messages.parse()` + `zodOutputFormat()`** (from `@anthropic-ai/sdk/helpers/zod`), not a hand-prompted "return only valid JSON" + manual `JSON.parse()` — the `claude-api` skill's TypeScript docs recommend this as the modern structured-output path, and it returns an already-schema-validated `parsed_output`. Model is fixed to `claude-opus-5` per the skill's own non-negotiable default. **`temperature` is never passed** — `claude-opus-5` runs adaptive thinking by default and rejects sampling params (`temperature`/`top_p`/`top_k`) with a 400 while thinking is on; determinism is controlled via `output_config.effort` (`"low"` for extraction/scoring) instead. This replaces `library-docs.md`'s old temperature-based guidance (0.3/0.7), which doesn't apply to this model at all.
- Extraction is scoped to what a resume can actually state: full name, phone, location, LinkedIn/portfolio URLs, current title, experience level, years of experience, skills, industries, work experience (max 3 roles), and education. Deliberately does **not** attempt to extract job preferences (remote preference, salary expectation, preferred locations, work authorization) since resumes don't typically state personal preferences — guessing those would be fabrication, not extraction. The button ("Extract from Resume" in `ResumeUpload`) only appears for a file just selected in this session, not a previously-saved resume — re-extracting from an already-uploaded resume would need to download it back first, which is out of scope for this feature.
- Extraction merges into the existing form state non-destructively (only overwrites a field when Claude actually returned a value), so it can't clobber something the user already typed with a blank.
- Verified for real, not mocked: generated a genuine PDF resume (via `cupsfilter`) and ran the actual `pdf-parse` v2 + `claude-opus-5` + `zodOutputFormat` pipeline directly against it (outside the auth-gated route, using the real `CLAUDE_API_KEY`). Every field extracted correctly, including inferring `experienceLevel: "senior"` from context and correctly parsing "Present" into `current: true` with no invented job-preference fields.
- **pdf-parse worker fix (post-Feature 07):** clicking "Extract from Resume" for real threw "Internal server error." Server logs showed the actual cause: `pdfjs-dist` (which `pdf-parse` v2 wraps) resolves its worker script relative to its own module location at runtime, but Turbopack bundles the route handler into `.next/dev/server/chunks/...`, rewriting that relative path so the sibling `pdf.worker.mjs` no longer exists where PDF.js looks for it ("Setting up fake worker failed"). This only showed up through the real bundled route — the earlier isolated `tsx` script that verified the extraction pipeline ran plain Node, with no bundler in the path, so it never hit this. Fixed with `serverExternalPackages: ["pdf-parse"]` in `next.config.ts` (the current, stable Next.js 16 option — confirmed against the bundled docs; it was `serverComponentsExperimentalPackages` pre-v15), which tells Next.js to leave the package unbundled and let Node resolve it natively instead. Re-verified for real: temporarily bypassed the route's auth check, `curl`'d the actual dev server with a real PDF (200, correct fields back), restored the auth check, confirmed it now 401s again.
- **Resume PDF Generation (Feature 08):** `app/api/resume/generate/route.ts` reads the **saved** profile from the DB (not the live, possibly-unsaved `ProfileForm` state) — Claude writes a professional summary + polishes each role's freeform notes into 2-4 bullet points (grounded only in given facts, explicitly told never to invent employers/titles/dates/accomplishments), then `components/resume/ResumePDF.tsx` (a `@react-pdf/renderer` `Document`, called as a plain function, never JSX, to keep the route a `.ts` file) renders a single A4 page, uploaded to the same `resumes/{user_id}/resume.pdf` path as manual uploads (consistent with the "one active resume per user" scope decision — generating always replaces, never creates a second file).
- Skills are passed through verbatim, never "polished" by Claude — only work-experience prose is rewritten; a skill list has no room for fabrication risk the way freeform bullet text does, so there's nothing for the model to add value to there.
- Confirmed `@react-pdf/renderer` needs no `serverExternalPackages` entry (unlike `pdf-parse`) — it's already in Next.js's own default-externalized list, verified against the bundled Next.js docs before assuming so.
- Verified for real, in three parts, without touching the account's real profile data: (1) a plain-Node script rendering `ResumePDF` directly — confirmed valid PDF output early; (2) the actual bundled route, temporarily fed a realistic mock profile in place of the DB read and writing the buffer to a scratch file instead of real storage — this specifically checks for a pdf-parse-style Turbopack bundling break, and confirmed there isn't one; parsed the resulting PDF back with `pdf-parse` and the rendered text was fully correct (right dates, grounded bullets, single page); (3) restored the route to its real form and confirmed the auth guard 401s again with no session. Real storage upload + `profiles.resume_pdf_url` update still need your own live login to exercise, same limitation as Features 06/07.
- **Find Jobs Page (Feature 09):** built pixel-matched against `context/designs/find-jobs.png`, mock data only (search/filter/sort inputs are interactive but not wired to real logic — that's Feature 11). One real spec-vs-image conflict, resolved in the image's favor per this session's established practice: `build-plan.md`'s written spec lists a Source (Search/URL) badge column, but the design image has no such column, and dropping it is actually more correct now — `jobs.source` is permanently `'search'` since Feature 04's schema decision (URL import out of scope), so a Source badge would show the identical value on every row forever. Also: `project-overview.md`'s "Jobs by Adzuna" credit isn't present anywhere in this design image, so it's omitted for now too, same reasoning as Feature 05's dropped Cover Letter Tone field.
- The 6 mock job rows (Vercel/Stripe/Linear/Notion/OpenAI/Figma, same scores/colors) are pixel-identical to the homepage's `JobSearchSection` marketing mock, confirmed by sampling — reused the same per-row color choices rather than re-deriving a score-to-color rule that doesn't actually exist (this was already known from the homepage build; re-confirmed here since a fresh read of the new design could have suggested it was a real formula).
- Verified visually with Playwright (temporarily removed `/find-jobs` from `proxy.ts`'s protected list, screenshotted, restored, confirmed the 307 redirect returned for an unauthenticated request afterward) — same pattern as Features 05/06. No console errors.
- This project doesn't use the `/scope` skill's `docs/scope/` tracking — `/architect` was told to keep this feature's build tasks in the spec's own `## Build plan` rather than create a parallel scope entry, since `build-plan.md`/`progress-tracker.md` already play that role here.
- **Adzuna Job Discovery (Feature 10):** planned via `/architect`-style plan mode (triggered by re-reading `library-docs.md`'s Adzuna section per your instruction), approved before building. Makes the "Find Jobs" button in Feature 09's UI real for the first time: `POST /api/agent/find` runs `agent/adzuna.ts`'s `discoverJobs()` — Adzuna search (`lib/adzuna.ts`, `detectCountry()` keyword heuristic: uk/london→gb, australia/sydney→au, canada/toronto→ca, else us) → per-result Claude scoring (`agent/matcher.ts`'s `scoreJob()`, `claude-opus-5`, `effort: "low"`, grounded `matchedSkills`/`missingSkills` that must come from literal profile/job text) → per-job insert into `jobs` (not one bulk insert — Adzuna caps at 10 results, so already-scored jobs survive even if a later one in the batch fails).
- **Scope decision, confirmed with you first:** `app/find-jobs/page.tsx` now shows real jobs immediately after Feature 10 rather than waiting for Feature 11 — it's an async Server Component fetching the signed-in user's real `jobs` rows (`order("found_at", { ascending: false })`, no filter/sort/pagination params — that's still 100% Feature 11's job, not silently pulled forward). `JobsTable` changed from a self-contained mock component to `{ jobs: JobRow[] }`-driven, with `MOCK_JOBS` removed.
- **Real match-score color now follows `ui-tokens.md`'s documented rule** (≥70 success, 50–69 warning, <50 muted) instead of Feature 09's illustrative green/blue/orange mock split (confirmed earlier this session to be decorative, reused pixel-for-pixel from the homepage mock, not a real formula) — the mock's "info"/blue tier is gone for real data.
- **One bad job never fails the whole batch**: if Claude's scoring call fails for one Adzuna result, that job is skipped and logged to `agent_logs` (`level: 'error'`, via new `agent/log.ts`'s `logAgentError()`, which resolves the current user itself rather than taking a `userId` param) — the rest of the batch still gets scored and saved, per `code-standards.md`'s "never let one failure crash the run."
- **Profile gate is a non-empty `skills` array, not full `is_complete`** — a search only needs something to match against; requiring a fully complete profile (phone, education, etc.) before letting someone search felt too strict for a first pass and can be tightened later. Route returns 422 with a clear message if `skills` is empty, and only creates the `agent_runs` row *after* this validation passes, so a bad/missing profile never logs a spurious `failed` run.
- Extracted `app/profile/page.tsx`'s `ProfileRow`/`mapRowToProfile()`/`emptyProfile()` into new `lib/profile.ts` (behavior-identical refactor) so `app/api/agent/find/route.ts` can reuse them instead of duplicating ~50 lines.
- `agent/extractor.ts` (structured job-description fields: responsibilities/requirements/benefits) stayed out of scope — no numbered feature currently assigns it work; Feature 12 (Job Details Page) only needs the raw Adzuna description snippet.
- **`.env.local` has a typo that will silently break this feature for you**: `AZUNA_API_KEY` should be `ADZUNA_APP_KEY` per `code-standards.md`'s env var table — flagged, not auto-fixed, since `.env.local` is gitignored and user-owned. **You need to rename this key before testing a real search.** For my own verification I used a shell-only env override so I never touched your file.
- Small robustness fix while building: `agent/log.ts`'s `agent_logs` insert now checks and `console.error`s its own `insertError` instead of silently swallowing a failed log write.
- Verified in layers, same methodology as Features 06/07/08: (1) an isolated `tsx` script called the real Adzuna API and real Claude directly with zero DB/auth involved — got 10 real results back, Claude scored one at a grounded 82% match with correct matched/missing skill lists; (2) the real bundled route with a temporary auth-bypass and mocked profile/DB writes — confirmed a 49-second run (proving all 10 jobs were scored sequentially via real Claude calls, not stubbed), plus a validation check (empty `jobTitle` → 400) and a nonsense-title check (zero results → `success: true` with zeros, not an error); (3) restored the route to production form, confirmed `tsc --noEmit` and `next build` both clean, and confirmed unauthenticated requests 401 again. The fully real, logged-in path (real RLS-protected `agent_runs`/`jobs` rows, real PostHog events under your real `userId`) still needs your own live login to exercise — same limitation as every AI-integration feature this session.
- **Between Features 10 and 11, fixed a real-world performance bug you reported**: `agent/adzuna.ts`'s `discoverJobs()` scored the up to 10 Adzuna results with sequential Claude calls (one full round trip after another), which is what made a real search take 30–50 seconds. Changed to score all jobs concurrently via `Promise.all` (and insert all in parallel too), so a search now takes roughly as long as the slowest single Claude call rather than the sum of all of them. No behavior change — same skip-and-log-on-failure semantics per job, just parallel instead of sequential.
- **Filter + Sort + Pagination (Feature 11):** wired `app/find-jobs/page.tsx` to real `?q=&match=&sort=&page=` URL search params — the standard App Router pattern (`code-standards.md`'s "data fetching happens in Server Components" rule), not client-side fetching. All filtering/sorting/pagination happens in one InsForge query: `.eq(user_id)` always; `match=high` → `.gte("match_score", MATCH_THRESHOLD)`, `match=low` → `.lt(...)`; `q` → `.or('company.ilike.%q%,title.ilike.%q%')` (case-insensitive, ANDed with the match filter — confirmed this is how the SDK's `.or()` combines with prior filter calls, not a separate OR'd-in user_id risk, since `.eq(user_id)` is always applied); sort → `.order("match_score"|"found_at", ...)`; pagination → `.range(from, to)` with `select("*", { count: "exact" })` for the real total. New `JOBS_PAGE_SIZE = 20` constant in `lib/utils.ts`, next to `MATCH_THRESHOLD`, per the build plan's "20 jobs per page" spec.
- **`JobFilters.tsx`** (still `"use client"`, unchanged interaction shape) now pushes URL changes via `useRouter`/`usePathname` instead of only holding local state: dropdowns navigate immediately on change, the text search debounces 400ms before navigating and resets to page 1 (by omitting the `page` param) on any filter change. Added a sync effect so the input reflects the URL on back/forward navigation, guarded so it doesn't re-trigger its own debounce loop.
- **`JobsPagination.tsx` changed from a static client mock to a real Server Component** (no `"use client"` needed — pure `<Link>`-based navigation, no event handlers) taking `currentPage`/`totalPages`/`totalCount`/`pageSize`/`baseParams` (the current `q`/`match`/`sort` querystring, built once in `page.tsx`, so page links preserve active filters). Windowed page-number display (first two, last two, current ±1, `...` elsewhere) instead of a hardcoded `[1,2,3]`; the whole Prev/page-numbers/Next row is hidden when `totalPages <= 1` rather than showing disabled controls for nothing to paginate.
- Verified by temporarily bypassing both `proxy.ts`'s redirect and the page's own auth redirect (same layered pattern as every prior feature), hitting `/find-jobs` with every filter/sort/page combination including edge cases (a comma in the search text, `page=999` beyond the last page) — all returned 200 with no query-builder errors, and the empty state / "Showing 0 to 0 of 0 results" rendered correctly under RLS with a fake user id. Restored both bypasses, confirmed the 307 redirect returns for unauthenticated requests again, and `tsc --noEmit` / `next build` are clean. The fully real path (your own real jobs, actually filtering/sorting/paging through them) still needs your live login to exercise.
- **Job Details Page — Full UI (Feature 12):** built pixel-matched against `context/designs/job-details.png`, real data end to end since the `jobs` table has been populated since Feature 10 — no mock-data phase needed for this one, per the build plan's own instruction ("Job data from DB is already available from Phase 3 — wire real data for all job info and match sections immediately"). New dynamic route `app/find-jobs/[id]/page.tsx` fetches one job row scoped to `id` AND `user_id` (belt-and-suspenders on top of RLS), `notFound()` if absent — covers both "doesn't exist" and "belongs to someone else."
- **Component split differs from `architecture.md`'s original placeholder tree** (`JobInfo`/`MatchScore`/`JobDescription`/`CompanyResearch`/`JobActions`) — built as `JobHeader`/`JobInfoCards`/`MatchReasoning`/`SkillsComparison`/`JobDescription`/`CompanyResearch` instead, one component per visual section in the actual design image (same granularity precedent as Feature 09's find-jobs/ split). No `JobActions.tsx` — "Back to Jobs" and "Apply Now" are single-element, page-specific pieces inlined directly in `page.tsx` rather than split into their own file. `architecture.md`'s tree updated to match what was actually built.
- **Match-score badge in the header uses a 2-tier system, not JobsTable's 3-tier one**: `ui-tokens.md`'s "Status Badges" table already defines exactly this — High Match (`bg-success-lightest`/`text-success-foreground`) vs Low Match (`bg-surface-secondary`/`text-secondary`), threshold at `MATCH_THRESHOLD` (70) — reused directly rather than inventing a third "warning" tier for a badge, since no light-warning-background token exists in `ui-tokens.md` (unlike the score bar's fill color, which does have one).
- **Gap skills use `ui-tokens.md`'s documented Skills Badges spec** (`bg-accent-muted`/`text-accent`, light purple) — the design image confirms this, not the build-plan prose's "red/orange badges" description. Same image-over-prose resolution pattern as Features 05/09.
- **Extracted `agent/adzuna.ts`'s `mapJobRow()` into new `lib/jobs.ts`** (behavior-identical refactor) so the details page can reuse it instead of duplicating the row-mapping — same precedent as Feature 10's `lib/profile.ts` extraction (Decision 8).
- **Job Type shows "—" when null**, Location/Salary show "Not listed" when null, matching the design image's own empty-state rendering exactly (the design's example job happens to have no `job_type`) — defensive fallbacks even though `agent/adzuna.ts` currently always sets a job_type value.
- **Company Research card is empty-state only, as scoped** — the "Research Company" button renders fully styled per the design but has no `onClick` yet; Feature 13 owns wiring it to `POST /api/agent/research`.
- Verified by temporarily bypassing `proxy.ts`'s redirect and swapping in a hardcoded mock row matching the design's exact "Backend Developer at Insight Global, 85% match" example (same layered pattern as every prior feature) — confirmed 200 with all sections rendering correct real values (85% badge, salary, skills, description, empty research state), then confirmed the not-found path (`notFound()` → real 404) for a nonexistent job id under real RLS. Restored both bypasses, confirmed the 307 redirect returns for unauthenticated requests again, and `tsc --noEmit` / `next build` are clean. Your own real job rows still need your live login to view.
- **Company Research Agent (Feature 13):** planned via `/architect` (spec `docs/specs/0002-company-research-agent.md`, status now `Accepted`) — this is the first feature this session to go through the actual `/architect` skill rather than plan mode, matching Feature 04's precedent. No new database migration — reuses `jobs.company_research` (already existed since Feature 04) and `agent_logs` (error logging reuses the job's own existing `run_id` rather than creating a new `agent_runs` row, since company research isn't a search run).
- **New `docs/specs/0002-company-research-agent.md`** records the design decisions: synchronous one-request pipeline (no background job — matches every other agent route this session, and the engineer's own answer that a simple inline spinner is enough), re-research always allowed and overwrites, a total-failure (not just thin browser research) shows a real error banner rather than faking success, `sources` is the real URLs visited rather than paraphrased labels.
- **`@browserbasehq/sdk` and `@browserbasehq/stagehand` were approved dependencies but never actually installed until this feature** — `npm install`ed for the first time here. `agent/research.ts`, `lib/browserbase.ts`, `lib/stagehand.ts` are new; `agent/adzuna.ts`'s `mapJobRow()` extraction (Feature 12) is now also used by this feature's route via `lib/jobs.ts`.
- **`library-docs.md`'s Browserbase/Stagehand sections were completely stale** (assumed a different, older SDK version: `new Stagehand({ env, browserbaseSessionID, ... })`, object-argument `extract({ instruction, schema })`, `openai/gpt-4o` for the synthesis call) — rewritten from the real installed v4 package's `.d.mts` type definitions and real end-to-end testing, same "package's own types are more authoritative than the doc draft" lesson as Feature 02's InsForge correction.
- **Real bugs caught by testing against a real company site (Stripe) before this shipped**, not assumptions: (1) `pageLinks[].url` came back as visible link text ("Careers", "Blog") instead of real hrefs on the first pass — fixed by explicitly telling the schema/instruction to return the href, not the label; (2) the Claude synthesis call truncated mid-JSON and threw a parse error at `max_tokens: 2048` on a content-rich company — bumped to `4096`, confirmed clean on retest; (3) a zod version mismatch between the project's own `zod` and Stagehand's exact-pinned bundled `zod` silently broke `extract()`'s typed overload — fixed by pinning the project's own `zod` to the exact same version (`4.4.3`) in `package.json`; (4) Stagehand's own model catalog doesn't yet list `claude-opus-5` — used `claude-sonnet-5` for Stagehand's internal page-reading model only, `lib/claude.ts`'s own calls (the actual dossier synthesis) stay on `claude-opus-5` as always; (5) bundling Stagehand into a Turbopack route chunk broke its own asset-path resolution — fixed with `serverExternalPackages: ["@browserbasehq/stagehand"]`, same class of fix as `pdf-parse`'s Feature 07 bug.
- **Real measured latency for a content-rich company (Stripe, multiple sub-pages actually visited) was ~111 to 120 seconds**, not the "20 to 60 seconds" estimated in the spec before real testing — `library-docs.md` and the Company Research card's loading copy are sized to this reality now.
- Verified in layers per this session's established methodology: (1) an isolated `tsx` script (moved into the project directory as `.mts` — a bare `tsx` in a scratchpad directory outside the project can't resolve `node_modules`, and this package is ESM-only so a plain `.ts` extension hit the same `ERR_PACKAGE_PATH_NOT_EXPORTED` class of issue as Feature 10's InsForge test) ran the real pipeline against `stripe.com` twice — once catching the two real bugs above, once confirming both fixes; (2) the real bundled route with a temporary auth/DB bypass, hit through `curl` with a generous timeout — confirmed the actual `agent/research.ts` code path (not just the isolated script) produces the same correct, grounded dossier through Turbopack; (3) restored both bypasses, confirmed `tsc --noEmit` and `next build` clean, and the fast-fail paths (400 missing jobId, 401 unauthenticated) still return correctly. The fully real, logged-in path (your own real job, real RLS-protected `company_research` write, a real `company_researched` PostHog event under your real `userId`) still needs your own live login to exercise.
- **Follow-up polish on Feature 13 (requested after shipping):** the Company Research dossier's `Your Edge`/`Gaps to Address`/`Smart Questions`/`Interview Prep` (and every other) field now renders as its own nested card (`DossierSection`, one level of nesting inside the outer card — the max `ui-rules.md` allows) with a colored icon ahead of each label, instead of plain stacked text blocks. Improvised (no design image exists for the populated dossier state) — all icon/color choices reuse existing `ui-tokens.md` tokens, no new hex values.
- **Dashboard Page — Full UI (Feature 14):** built from `build-plan.md`'s explicit UI list, **not** `context/designs/job_dashboard_ui.webp`. That image turned out to be a mismatched asset — a different, unrelated product ("atica" branding, blue theme, left sidebar nav) that directly contradicts `ui-rules.md`'s "top navbar only, no sidebar" rule and `ui-tokens.md`'s purple accent. Flagged and confirmed with you before building; you chose to ignore the image and build from the written spec, same resolution mechanism this session uses for image-vs-prose conflicts, just inverted (prose wins because the image isn't actually this product's design).
- **Stat labels follow `build-plan.md`'s Feature 14 list exactly** (Total Jobs Found, Avg. Match Rate, Companies Researched, Cover Letters Generated) rather than the homepage `DashboardPreview` mock's differing 4th stat ("Jobs This Week") — the two mocks are allowed to diverge since they serve different purposes (marketing teaser vs. the real page), and build-plan.md is the explicit, unambiguous source for the real page.
- **Incomplete profile banner uses real data, not mock** — build-plan.md's Feature 14 line for it doesn't say "(mock)" the way the stat cards/activity/charts explicitly do, and the underlying check (`computeProfileCompletion(profile).isComplete`) was already built and cheap to call, matching Feature 12's "wire real data immediately where it's already available" precedent. Stat numbers, Recent Activity entries, and all three charts stay mock — their real wiring is Features 15/16/17 respectively, per `build-plan.md`.
- **No charting library installed.** `build-plan.md`'s Feature 17 (real PostHog data) names `recharts`, but nothing in the approved-dependencies list (`code-standards.md`) includes it yet, and Feature 14 is UI-only. Hand-rolled all three charts with Tailwind divs (bars) and inline SVG (the line chart, gradient fill, dashed grid lines — matching `ui-tokens.md`'s Dashboard Chart Colors spec exactly), following the same technique the homepage's `DashboardPreview` mock already established. Whether to introduce `recharts` is deferred to Feature 17, when real, dynamically-shaped data actually needs it.
- **Recent Activity mock uses all 3 dot colors from `ui-tokens.md`'s Activity Dots table** (accent/resume tailored, info/company research, success/job found) as a clean, self-consistent type→color mapping — unlike the homepage `DashboardPreview` mock, which used the same message pattern ("Found X jobs") with two different dot colors in different rows (confirmed decorative, not a real formula, earlier this session). Flagged for Feature 16: only the info and success types will ever come from real agent data in this build plan; the accent/"resume tailored" type has no agent behind it and will stay mock-only unless a future feature adds one.
- Verified by temporarily bypassing both `proxy.ts`'s redirect and the page's own auth redirect (same layered pattern as every prior feature), rendering with an empty mock profile to confirm the incomplete-profile banner shows, and grep-confirming every stat label, activity entry, chart title, and score-range label renders in the real HTML output (including the SVG line chart's gradient). Restored both bypasses, confirmed the 307 redirect returns for unauthenticated requests again, and `tsc --noEmit` / `next build` are clean. Your own real profile-completion state still needs your live login to see reflected.
- **Stats Bar — Real Data (Feature 15):** found and resolved a real inconsistency between `build-plan.md`'s own Feature 14 and Feature 15 sections — Feature 14's mock stat list names the 4th card "Cover Letters Generated," but Feature 15's real-data logic explicitly names "Jobs This Week" with a concrete formula (count of jobs found in the last 7 days) instead. No feature anywhere in this 17-feature build plan generates cover letters, so "Cover Letters Generated" could never be wired to real data; "Jobs This Week" also matches the homepage `DashboardPreview` mock's original 4th stat. Resolved in favor of "Jobs This Week" — swapped the `StatsBar` card (icon, label, formula) rather than keeping an unwireable mock stat forever.
- **Four real, parallel queries in `app/dashboard/page.tsx`** (via `Promise.all`, same pattern as Feature 11's find-jobs query): Total Jobs Found and Jobs This Week and Companies Researched use `select("*", { count: "exact", head: true })` (count-only, no row data fetched); Companies Researched adds `.not("company_research", "is", null)` (PostgREST's real `not.is.null` filter syntax); Avg. Match Rate fetches just the `match_score` column for the user's jobs and averages it in application code, since there's no aggregate `AVG()` exposed through the PostgREST-style query builder and adding a DB view for one stat isn't worth the new migration.
- **Dropped the mock's fabricated trend badges** ("+12%", "+3%") — Feature 15's spec gives a formula for each of the four raw numbers but never a week-over-week trend calculation, and computing one for real would need a stored prior-period snapshot this project doesn't have. Showing a made-up percentage next to real numbers would be actively misleading, so each card now shows a plain descriptive note ("All time," "Across all jobs," etc.) instead — consistent with this project's broader "never invent" stance (the same principle governing Claude's research-synthesis prompts) applied to UI copy, not just AI output.
- **Avg. Match Rate shows "—" when the user has zero jobs** (or zero jobs with a non-null `match_score`), never a fabricated "0%" — an empty average is not the same as a real zero score.
- Verified by temporarily bypassing both `proxy.ts`'s redirect and the page's own auth redirect, rendering with a fake user id under real RLS (all four queries correctly returned empty/zero results, no query-builder errors) and confirming the exact rendered values (`0`, `—`, `0`, `0`) in the real HTML output. Restored both bypasses, confirmed the 307 redirect returns for unauthenticated requests again, and `tsc --noEmit` / `next build` are clean. Your own real stat numbers still need your live login to see.
- **Recent Activity — Real Data (Feature 16):** found a real data-model gap before writing the query — `jobs.found_at` records when a job was *discovered* via Adzuna search, not when it was researched, and build-plan.md's "Researched {company} — {time ago}" entry needs the research time specifically (research routinely happens well after a job is first found). Added `jobs.researched_at timestamptz` (nullable, plus a matching index) via a direct additive migration rather than a full `/architect` pass — a single non-breaking column, same judgment call as Feature 15's stat-list fix (see `docs/specs/0001-core-data-schema.md`'s Follow-up for the full note). `app/api/agent/research/route.ts` now sets it alongside `company_research` on every successful (and re-)research.
- **Merge logic**: two parallel queries — `agent_runs` where `status = 'completed'` (a `'running'`/`'failed'` run doesn't fit the "Found X jobs" message shape), and `jobs` where `researched_at IS NOT NULL` — each fetched newest-first and capped at 5, combined into one list, sorted by timestamp, then sliced to the final 5 shown. Matches `build-plan.md`'s "merge and sort all by created_at descending, take last 5-10" logic, using `completed_at` (falling back to `started_at`) for runs and `researched_at` for research entries as the sort key.
- **Dropped the "resume tailored" (accent) mock entries entirely** — flagged as mock-only back in Feature 14 (no agent in this build plan produces that event) and now confirmed: only the two real entry types (`success` for a completed search, `info` for a completed research) exist. `RecentActivity.tsx`'s dot-color mapping is unchanged from Feature 14, just narrower.
- **Real empty state**: "No activity yet — search for jobs or research a company to see it here." shown when the merged list is empty, per `ui-rules.md`'s "every section that can be empty must have an empty state" rule — confirmed rendering correctly under a fake user id with real RLS blocking all rows.
- Verified by temporarily bypassing both `proxy.ts`'s redirect and the page's own auth redirect, rendering under a fake user id with real RLS (both new queries returned zero rows, no query-builder errors) and confirming the real empty-state copy renders, alongside every other dashboard section still working correctly. Restored both bypasses, confirmed the 307 redirect returns for unauthenticated requests again, and `tsc --noEmit` / `next build` are clean. Your own real activity feed still needs your live login (and at least one completed search or research run) to see populated.

## Notes

- No shadcn/ui components installed yet — homepage uses plain Tailwind markup only. `lucide-react` was installed (already an approved dependency) for icons. lucide-react ships no brand icons (Google/GitHub) — the login page uses inline SVGs for those.
- Verified visually via Playwright screenshot against `context/designs/landing-page.png` — no console errors on load.
- `@insforge/sdk` installed (replaces the incorrectly-assumed `@insforge/ssr`). Real anon key + backend URL are in `.env.local` (gitignored). Google and GitHub OAuth are confirmed enabled on the backend via `get-backend-metadata`.
